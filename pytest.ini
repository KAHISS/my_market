[pytest]
DJANGO_SETTINGS_MODULE = my_market.settings
python_files = tests.py test_*.py *_tests.py test.py
addopts = 
    --doctest-modules
    --strict-markers
markers = 
    slow: run tests that are slow
    fast: run tests that are fast
    functional_test: run functional tests with selenium
